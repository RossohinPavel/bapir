// Класс для тестов на замену BX24Wrapper
class BX24Wrapper {
    static createCalls(endpoint, requests) {
        return [];
    }

    async callLongBatch(calls, flag) {
        return [];
    }

    async callMethod(endpoint, params) {
        return {};
    }

    async callListMethod(calls, flag) {
        return [];
    }
}
